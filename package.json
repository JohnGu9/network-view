{
  "name": "network-view",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build:web && npm run build:mid && npm run build:rust",
    "build:web": "cd web && npm i && npm run build",
    "build:mid": "npm i && ts-node build.ts",
    "build:rust": "cargo build --release"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "ts-node": "^10.9.1"
  }
}
